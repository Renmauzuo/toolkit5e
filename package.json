{
  "name": "toolkit5e",
  "version": "1.0.1",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "tsc --build",
    "watch": "tsc --build --watch",
    "test": "vitest --run"
  },
  "devDependencies": {
    "typescript": "^6.0.2",
    "vitest": "^0.34.6"
  }
}
