{
  "name": "toolkit5e",
  "version": "1.0.1",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "tsc --build",
    "test": "echo \"No test specified\" && exit 0"
  },
  "devDependencies": {
    "typescript": "^6.0.2"
  }
}
